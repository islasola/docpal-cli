{
  "name": "@islasola/docpal",
  "version": "0.1.0",
  "private": true,
  "description": "DocPal monorepo — CLI, Claude Code plugin, and skills for Feishu↔Docusaurus documentation pipelines",
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "smoke": "pnpm --filter @islasola/docpal-cli smoke",
    "smoke:docs": "pnpm --filter @islasola/docpal-cli smoke:docs",
    "lint": "turbo run lint",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2",
    "turbo": "^2.5.2"
  },
  "packageManager": "pnpm@10.10.0"
}
